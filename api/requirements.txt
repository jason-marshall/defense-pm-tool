# Defense PM Tool API - All Dependencies
# For production only, use: pip install -r requirements/base.txt
# For testing only, use: pip install -r requirements/test.txt
# For development, use: pip install -r requirements/dev.txt
# This file installs everything (backward compatible):
-r requirements/dev.txt
-r requirements/test.txt
